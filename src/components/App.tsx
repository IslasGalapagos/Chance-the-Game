import * as React from 'react';
import Bg from './Bg';

class App extends React.PureComponent<{}, {}> {
  render() {
    return (
      <React.Fragment>
        <Bg />
      </React.Fragment>
    );
  }
}

export default App;
